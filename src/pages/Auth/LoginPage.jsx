import { Login } from "../../components/Login/Login";

export const LoginPage = ()=>{
    return(
        <div className="LoginPage h-screen flex items-center justify-center bg-gradient-to-l from-gray-900 to-gray-800">
            <Login/>
        </div>
    );
}